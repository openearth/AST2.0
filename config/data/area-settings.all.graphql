query ($locale: SiteLocale) {
  areaSettings: allAreaSettings(locale: $locale) {
    title,
    infoText,
    key
    multiple,
    isSelect,
    options {
      title,
      value
    },
    defaultValue {
      title,
      value,
    },
  }
}
