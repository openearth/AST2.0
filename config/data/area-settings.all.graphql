query ($locale: SiteLocale) {
  areaSettings: allAreaSettings(locale: $locale) {
    title,
    key
    multiple,
    isSelect,
    options {
      title,
      value
    },
    defaultValue {
      title,
      value,
    },
  }
}
