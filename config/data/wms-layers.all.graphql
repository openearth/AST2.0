query ($locale: SiteLocale) {
  wmsLayers: allWmsLayers(locale: $locale){
    id,
    title,
    layerType,
    tilesize,
    baseurl,
    layers,
    version,
    tilesize,
    bbox,
    format,
    service,
    srs,
    transparent,
    styles
  }
}
